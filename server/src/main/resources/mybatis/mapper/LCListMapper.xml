<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "%7e//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="freshmanGuide.attendanceCheck.mapper.LCListMapper">
    <insert id="save" parameterType="freshmanGuide.attendanceCheck.DTO.BasicDTO$LCInfoDTO">
        INSERT INTO lc_list
        VALUES (#{year}, #{lc}, #{fgMemberId1}, #{fgMemberId2})
    </insert>

    <select id="findByYearAndLc"
            parameterType="Integer"
            resultType="freshmanGuide.attendanceCheck.DTO.BasicDTO$LCInfoPKDTO"
    >
        SELECT *
        FROM lc_list
        WHERE year=#{year} AND lc=#{lc}
    </select>

    <delete id="deleteByYearAndLc" parameterType="freshmanGuide.attendanceCheck.DTO.BasicDTO$LCInfoPKDTO">
        DELETE FROM lc_list
        WHERE year=#{year} AND lc=#{lc}
    </delete>
</mapper>